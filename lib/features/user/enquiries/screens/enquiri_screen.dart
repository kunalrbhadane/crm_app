import 'package:flutter/material.dart';

class EnquiriScreen extends StatelessWidget {
  const EnquiriScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Enquiry Screen'),
      ),
    );
  }
}